body, html {
    width: 100%;
    height: 100%;
    margin: 0;
    padding: 0;
    overflow: hidden;
    font-size: 14px;
    background: url("../images/noisy_grid.png") #fff;
}

.map {
    width: 100%;
    height: 100%;
    margin: 0;
    padding: 0;
    overflow: hidden;
}

.homeButton {
  position: absolute;
  top: 85px;
  left: 10px;
  z-index: 30;
}